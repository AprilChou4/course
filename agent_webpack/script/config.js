module.exports = {
  // 缓存目录配置，无需修改
  prevJSPath: '/dist_prev/script/',
  prevCSSPath: '/dist_prev/style/',

  // 打包前 临时 文件所在目录，无需修改
  tempJSPath: '/dist_temp/script/',
  tempCSSPath: '/dist_temp/style/',

  // 打包后 dist 文件夹中 js 文件所在目录，按需修改，末尾带 /
  distJSPath: '/dist/script/',
  // 打包后 dist 文件夹中 css 文件所在目录
  distCSSPath: '/dist/style/',

  // tags 文件夹中 js 文件所在目录，按需修改，末尾带 /
  tagJSPath: '/dist_tags/script/',
  // tags 文件夹中 css 文件所在目录
  tagCSSPath: '/dist_tags/style/',
};
